<!DOCTYPE html>
<html>
    <head>
        <title>Idealista TEST</title>
    </head>
    <body>
        <ul>
            {% for ad in ads %}
            <li>ID: {{ad.id}} - Points: {{ad.points}}</li>
            {% endfor %}
        </ul>
    </body>
</html>