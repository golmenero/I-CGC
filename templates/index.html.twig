<!DOCTYPE html>
<html>
    <head>
        <title>Idealista TEST</title>
    </head>
    <body>
        <ul>
            {% for ad in ads %}
            <li>{{ad.points}}</li>
            {% endfor %}
        </ul>
    </body>
</html>